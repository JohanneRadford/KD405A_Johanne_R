
public class Human {
	
	
	private Dog dog;
	private String name;
	private String info;
	private String NullDog;

	public Human(String name) {
		this.name = name;
		
		/**	
		if (name.length() > Constants.MIN_NUMBER_OF_LETTERS){
		Human.name = name;
		
		} if (name.length() < Constants.MIN_NUMBER_OF_LETTERS && name.length()>1 ){
			Human.name = "navn må være mer enn tre bokstaver";
			
		} if (name.length() < 1){
			Human.name = "hunden må ha en eier";
		
		
		}*/
	
	}
	
	public String getName(){
		return name;
	}
	
	
	public Dog buyDog(Dog dog1){
		this.dog = dog1;
		return dog;
			
	}
	
	public String getInfo(String info){
		if(dog == null){
			info = name + "has no dog";
		} else {
			info = name + " is buing a dog named " + dog.getName();
		}
		return info;
	
	}

}
